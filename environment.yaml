name: indexpaper
channels:
  - conda-forge
  - bioconda
  - pytorch
  - plotly
  - defaults
dependencies:
  - python=3.10
  - setuptools
  - pytorch
  - jupyter #for intellij jupyter only
  - jupyterlab #for examples only
  - pip:
      - pycomfort>=0.0.13
      - pynction
      - polars>=0.19.6
      - beartype
      - click
      - twine
      - tiktoken
      - openai
      - chromadb
      - langchain>=0.0.306
      - qdrant-client>=1.5.0
      - sentence_transformers
      - datasets
